# Dynamic_Project_Memory
A reusable, agent-agnostic memory system that gives AI coding assistants (Cursor, Claude Code, Codex) persistent project context across sessions — using tiered Markdown files instead of re-uploading docs or burning context windows.
